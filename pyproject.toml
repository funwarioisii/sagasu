[tool.poetry]
name = "sagasu"
version = "0.1.4"
description = "sagasu all my contents"
authors = ["funwarioisii"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry.scripts]
sagasu = 'sagasu.app:app'
